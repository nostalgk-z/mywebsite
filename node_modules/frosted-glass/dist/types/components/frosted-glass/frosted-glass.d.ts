export declare class FrostedGlass {
    el: HTMLElement;
    uuid: string;
    overlayColor: string;
    blurAmount: string;
    blurOffsetLeft: number;
    blurOffsetTop: number;
    scrollOffset: number;
    isFixed: boolean;
    updateBackground(): void;
    private blurContainer;
    private blurContent;
    private container;
    private ticking;
    componentDidLoad(): void;
    componentDidUnload(): void;
    onScroll(): void;
    onResize(): void;
    render(): JSX.Element[];
    private cloneUsingDivs(original);
    private initListeners();
    private removeListeners();
    private blurContentUpdate();
    private removeSelected(element, selectors);
    private requestTick(functionName);
}
