import { BuildCtx, CompilerCtx, Config, Manifest } from '../../declarations';
export declare function upgradeCollection(config: Config, compilerCtx: CompilerCtx, buildCtx: BuildCtx, manifest: Manifest): Promise<void>;
